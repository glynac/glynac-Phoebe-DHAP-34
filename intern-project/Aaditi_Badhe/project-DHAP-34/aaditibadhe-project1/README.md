# Airflow Docker Environment

## Prerequisites
- Docker + Docker Compose installed
- Python 3.9+ (for local scripts)

## Usage
```bash
# Start environment
docker compose up -d

# Stop environment
docker compose down
