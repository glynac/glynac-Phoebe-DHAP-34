# Silver Layer - Mautic Lead Event Log (AUTO MODE)
version: "1.0"
single_tenant: true

dag:
  description: "Transform Mautic lead_event_log from Bronze to Silver"
  tags: ["silver", "mautic", "lead_event_log", "auto-mode", "single-tenant"]

source:
  database: mautic
  table: lead_event_log_raw

target:
  database: mautic_silver
  table: lead_event_log

task:
  incremental:
    partition_column: partition

clickhouse:
  partition_by: toYYYYMM(partition)
  ttl: "partition + INTERVAL 730 DAY"

dedupe:
  partition_by: [id, partition]
  order_by: [date_triggered DESC]

filters:
  - "id IS NOT NULL"

lookback_days: 90

tests:
  - type: row_count
    min: 1
    severity: error
