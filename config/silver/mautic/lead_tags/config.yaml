# Silver Layer - Mautic Lead Tags (AUTO MODE)
version: "1.0"
single_tenant: true

dag:
  description: "Transform Mautic lead_tags from Bronze to Silver"
  tags: ["silver", "mautic", "lead_tags", "auto-mode", "single-tenant"]

source:
  database: mautic
  table: lead_tags_raw

target:
  database: mautic_silver
  table: lead_tags

task:
  incremental:
    partition_column: partition

clickhouse:
  partition_by: toYYYYMM(partition)
  ttl: "partition + INTERVAL 730 DAY"

transforms:
  tag:
    transform: clean_string

dedupe:
  partition_by: [id, partition]
  order_by: [id DESC]

filters:
  - "id IS NOT NULL"

lookback_days: 90

tests:
  - type: row_count
    min: 1
    severity: error
