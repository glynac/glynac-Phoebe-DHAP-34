# Silver Layer - Mautic Lead Fields (AUTO MODE)
version: "1.0"
single_tenant: true

dag:
  description: "Transform Mautic lead_fields from Bronze to Silver"
  tags: ["silver", "mautic", "lead_fields", "auto-mode", "single-tenant"]

source:
  database: mautic
  table: lead_fields_raw

target:
  database: mautic_silver
  table: lead_fields

task:
  incremental:
    partition_column: partition

clickhouse:
  partition_by: toYYYYMM(partition)
  ttl: "partition + INTERVAL 730 DAY"

dedupe:
  partition_by: [id, partition]
  order_by: [id DESC]

filters:
  - "id IS NOT NULL"

lookback_days: 90

tests:
  - type: row_count
    min: 1
    severity: error
