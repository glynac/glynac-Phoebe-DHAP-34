# Silver Layer - OpenCATS EEO Ethnic Type (AUTO MODE)
version: "1.0"
single_tenant: true

dag:
  description: "Transform OpenCATS eeo_ethnic_type from Bronze to Silver"
  tags: ["silver", "opencats", "eeo_ethnic_type", "auto-mode", "single-tenant"]

source:
  database: opencats
  table: eeo_ethnic_type_raw

target:
  database: opencats_silver
  table: eeo_ethnic_type

task:
  incremental:
    partition_column: partition

clickhouse:
  partition_by: toYYYYMM(partition)
  ttl: "partition + INTERVAL 730 DAY"

dedupe:
  partition_by: [eeo_ethnic_type_id, partition]
  order_by: [eeo_ethnic_type_id DESC]

filters:
  - "eeo_ethnic_type_id IS NOT NULL"

lookback_days: 90

tests:
  - type: row_count
    min: 1
    severity: error
