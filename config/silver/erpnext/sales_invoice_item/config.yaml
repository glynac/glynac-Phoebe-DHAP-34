# Silver Layer - ERPNext Sales Invoice Item (AUTO MODE)
version: "1.0"
single_tenant: true

dag:
  description: "Transform ERPNext sales_invoice_item from Bronze to Silver"
  tags: ["silver", "erpnext", "sales_invoice_item", "auto-mode", "single-tenant"]

source:
  database: nexterp
  table: sales_invoice_item_raw

target:
  database: nexterp_silver
  table: sales_invoice_item

task:
  incremental:
    partition_column: partition

clickhouse:
  partition_by: toYYYYMM(partition)
  ttl: "partition + INTERVAL 730 DAY"

transforms:
  item_name:
    transform: clean_string
  description:
    transform: clean_string

dedupe:
  partition_by: [name, partition]
  order_by: [modified DESC]

filters:
  - "name IS NOT NULL"

lookback_days: 90

tests:
  - type: row_count
    min: 1
    severity: error
