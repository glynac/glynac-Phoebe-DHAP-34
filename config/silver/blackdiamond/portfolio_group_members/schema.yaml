version: "1.0"
layer: silver
database: blackdiamond_silver
table: portfolio_group_members

metadata:
  display_name: "Black Diamond Portfolio Group Members (Cleaned)"
  description: |
    Cleaned and transformed Black Diamond portfolio group members mapping from Bronze layer.
    Links portfolios to portfolio groups for organizational grouping and reporting.
  
  owner: ai-team
  domain: blackdiamond
  tags:
    - blackdiamond
    - portfolio_group_members
    - mapping
    - silver
  data_classification: internal
  contains_pii: false
  update_frequency: daily
  retention_days: 2555  # ~7 years for financial records
  upstream_dependencies:
    - blackdiamond.portfolio_group_members_raw
  downstream_dependencies: []

clickhouse:
  engine: ReplacingMergeTree
  engine_params:
    version_column: _loaded_at
  partition_by: toYYYYMM(processing_date)
  order_by:
    - processing_date
    - portfolio_group_id
    - portfolio_id
  primary_key:
    - portfolio_group_id
    - portfolio_id
  ttl: "processing_date + INTERVAL 2555 DAY"
  settings:
    index_granularity: 8192  
  indexes:
    - name: portfolio_id_idx
      expression: portfolio_id
      type: bloom_filter
      granularity: 1

columns:
  - name: portfolio_group_id
    type: String
    nullable: false
    description: "Portfolio group ID (foreign key)"
    primary_key: true
    example: "150001"
    
  - name: portfolio_id
    type: String
    nullable: false
    description: "Portfolio ID (foreign key)"
    primary_key: true
    example: "2000001"
    
  - name: glynac_organization_id
    type: LowCardinality(String)
    nullable: false
    description: "Organization identifier for data isolation"
    example: "org-abc123"
    
  - name: processing_date
    type: Date
    nullable: false
    description: "Date when data was processed (partition key)"
    partition_key: true
    example: "2025-11-18"
    
  - name: _loaded_at
    type: DateTime
    nullable: false
    description: "Timestamp when loaded into Silver layer"
    system_column: true
    example: "2025-11-18 15:30:45"
    
  - name: _source_table
    type: String
    nullable: false
    description: "Source table name"
    system_column: true
    example: "blackdiamond.portfolio_group_members_raw"
    
  - name: _source_timestamp
    type: Nullable(Date)
    nullable: true
    description: "Source processing date"
    system_column: true
    example: "2025-11-18"