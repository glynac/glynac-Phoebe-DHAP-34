# Gold: Dimension Account
# Auto-detects dependency from query.sql

dag_id: gold_dim_account
description: "Account dimension table for dashboards"

schedule_interval: dataset  # Auto-detects from query.sql

catchup: false
max_active_runs: 1

tags:
  - gold
  - dimension
  - account
  - auto-dependency

task:
  task_type: clickhouse_transformation

default_args:
  owner: data-engineering
  retries: 2
  retry_delay_minutes: 5

execution_timeout: 1800
