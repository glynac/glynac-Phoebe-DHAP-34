schedule_interval: "0 3 * * *"  # Daily at 3 AM (after Silver at 2 AM)

depends_on:
  - silver_salesforce_user
dependency_validation: warn

catchup: false
max_active_runs: 1

tags:
  - gold_layer
  - metrics
  - daily

execution_timeout: 1800  # 30 minutes

retries: 2
retry_delay: 300  # 5 minutes