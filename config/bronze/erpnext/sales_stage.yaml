version: "1.0"
layer: bronze
database: nexterp
view: sales_stage_raw

source:
  bucket: raw-data
  path: erpnext/sales_stage
  format: orc
  file_pattern: "data.orc"
  partitions:
    - name: partition
      type: Date

clickhouse:
  engine: S3
  settings:
    input_format_orc_allow_missing_columns: 1

description: "Raw ERPNext sales_stage data via sql-extractor"
tags: [bronze, erpnext, sales_stage, sql-extractor, auto-schema]
