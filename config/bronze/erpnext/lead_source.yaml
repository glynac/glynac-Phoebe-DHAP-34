version: "1.0"
layer: bronze
database: nexterp
view: lead_source_raw

source:
  bucket: raw-data
  path: erpnext/lead_source
  format: orc
  file_pattern: "data.orc"
  partitions:
    - name: partition
      type: Date

clickhouse:
  engine: S3
  settings:
    input_format_orc_allow_missing_columns: 1

description: "Raw ERPNext lead_source data via sql-extractor"
tags: [bronze, erpnext, lead_source, sql-extractor, auto-schema]
