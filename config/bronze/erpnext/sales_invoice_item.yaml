version: "1.0"
layer: bronze
database: erpnext
view: sales_invoice_item_raw

source:
  bucket: raw-data
  path: erpnext/sales_invoice_item
  format: orc
  file_pattern: "data.orc"
  partitions:
    - name: partition
      type: Date

clickhouse:
  engine: S3
  settings:
    input_format_orc_allow_missing_columns: 1

description: "Raw ERPNext sales_invoice_item data via sql-extractor"
tags: [bronze, erpnext, sales_invoice_item, sql-extractor, auto-schema]
