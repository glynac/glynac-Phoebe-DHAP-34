version: "1.0"
layer: bronze
database: mautic
view: migrations_raw

source:
  bucket: raw-data
  path: mautic/migrations
  format: orc
  file_pattern: "data.orc"
  partitions:
    - name: partition
      type: Date

clickhouse:
  engine: S3
  settings:
    input_format_orc_allow_missing_columns: 1

description: "Raw Mautic migrations data via sql-extractor"
tags: [bronze, mautic, migrations, sql-extractor, auto-schema]
