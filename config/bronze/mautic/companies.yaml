version: "1.0"
layer: bronze
database: mautic
view: companies_raw

source:
  bucket: raw-data
  path: mautic/companies
  format: orc
  file_pattern: "data.orc"
  partitions:
    - name: partition
      type: Date

clickhouse:
  engine: S3
  settings:
    input_format_orc_allow_missing_columns: 1

description: "Raw Mautic companies data via sql-extractor"
tags: [bronze, mautic, companies, sql-extractor, auto-schema]
