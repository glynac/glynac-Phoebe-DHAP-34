version: "1.0"
layer: bronze
database: mautic
view: roles_raw

source:
  bucket: raw-data
  path: mautic/roles
  format: orc
  file_pattern: "data.orc"
  partitions:
    - name: partition
      type: Date

clickhouse:
  engine: S3
  settings:
    input_format_orc_allow_missing_columns: 1

description: "Raw Mautic roles data via sql-extractor"
tags: [bronze, mautic, roles, sql-extractor, auto-schema]
