version: "1.0"
layer: bronze
database: blackdiamond
view: portfolio_group_members_raw

metadata:
  description: "Raw Black Diamond portfolio group members data from MinIO"
  owner: data-engineering
  tags:
    - bronze
    - blackdiamond
    - wealth-management
    - portfolio-group-members

source:
  type: minio
  bucket: black-diamond-data
  path: main-data/blackdiamond/normalized
  format: parquet
  file_pattern: portfolio_group_members.parquet
  partitions:
    - name: glynac_organization_id
      type: String
    - name: processing_date
      type: Date

clickhouse:
  engine: S3
  settings:
    input_format_parquet_allow_missing_columns: 1

schema:
  mode: auto

validation:
  enabled: true
  checks:
    - name: not_empty
      query: "SELECT count() > 0 FROM {database}.{table}"
