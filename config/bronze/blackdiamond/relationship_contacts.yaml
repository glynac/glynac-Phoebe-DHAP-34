version: "1.0"
layer: bronze
database: blackdiamond
view: relationship_contacts_raw

metadata:
  description: "Raw Black Diamond relationship contacts data from MinIO"
  owner: data-engineering
  tags:
    - bronze
    - blackdiamond
    - wealth-management
    - relationship-contacts

source:
  type: minio
  bucket: main-data
  path: blackdiamond/relationship_contacts
  format: parquet
  file_pattern: relationship_contacts.parquet
  partitions:
    - name: glynac_organization_id
      type: String
    - name: processing_date
      type: Date

clickhouse:
  engine: S3
  settings:
    input_format_parquet_allow_missing_columns: 1

schema:
  mode: auto

validation:
  enabled: true
  checks:
    - name: not_empty
      query: "SELECT count() > 0 FROM {database}.{table}"
