version: "1.0"
layer: bronze
database: blackdiamond
view: cost_basis_summary_raw

metadata:
  description: "Raw Black Diamond cost basis summary data from MinIO"
  owner: data-engineering
  tags:
    - bronze
    - blackdiamond
    - wealth-management
    - cost-basis

source:
  type: minio
  bucket: main-data
  path: blackdiamond/cost_basis_summary
  format: parquet
  file_pattern: cost_basis_summary.parquet
  partitions:
    - name: glynac_organization_id
      type: String
    - name: processing_date
      type: Date

clickhouse:
  engine: S3
  settings:
    input_format_parquet_allow_missing_columns: 1

schema:
  mode: auto

validation:
  enabled: true
  checks:
    - name: not_empty
      query: "SELECT count() > 0 FROM {database}.{table}"
