version: "1.0"
layer: bronze
database: opencats
view: activity_type_raw

source:
  bucket: raw-data
  path: opencats/activity_type
  format: orc
  file_pattern: "data.orc"
  partitions:
    - name: partition
      type: Date

clickhouse:
  engine: S3
  settings:
    input_format_orc_allow_missing_columns: 1

description: "Raw OpenCATS activity_type data via sql-extractor"
tags: [bronze, opencats, activity_type, sql-extractor, auto-schema]
