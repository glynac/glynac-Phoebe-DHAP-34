version: "1.0"
layer: bronze
database: opencats
view: eeo_ethnic_type_raw

source:
  bucket: raw-data
  path: opencats/eeo_ethnic_type
  format: orc
  file_pattern: "data.orc"
  partitions:
    - name: partition
      type: Date

clickhouse:
  engine: S3
  settings:
    input_format_orc_allow_missing_columns: 1

description: "Raw OpenCATS eeo_ethnic_type data via sql-extractor"
tags: [bronze, opencats, eeo_ethnic_type, sql-extractor, auto-schema]
