version: "1.0"
layer: bronze
database: opencats
view: user_raw

source:
  bucket: raw-data
  path: opencats/user
  format: orc
  file_pattern: "data.orc"
  partitions:
    - name: partition
      type: Date

clickhouse:
  engine: S3
  settings:
    input_format_orc_allow_missing_columns: 1

description: "Raw OpenCATS user data via sql-extractor"
tags: [bronze, opencats, user, sql-extractor, auto-schema]
