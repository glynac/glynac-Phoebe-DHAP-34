# ERPNext Users Table Extraction
# Partitioned by creation column

task_id: extract_users
job_name: sql-extractor

environment:
  TABLE_NAME: "tabUser"
  OUTPUT_PREFIX: "erpnext/users"
  PARTITION_COLUMN: "creation"
  PARTITION_FORMAT: "%Y-%m-%d"
  INCREMENTAL_MODE: "false"
