# Mautic Roles Table Extraction
# User role definitions

task_id: extract_roles
job_name: sql-extractor

environment:
  TABLE_NAME: "roles"
  OUTPUT_PREFIX: "mautic/roles"
  INCREMENTAL_MODE: "false"
