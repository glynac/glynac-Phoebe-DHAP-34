# Mautic Plugins Table Extraction
# Installed plugin registry

task_id: extract_plugins
job_name: sql-extractor

environment:
  TABLE_NAME: "plugins"
  OUTPUT_PREFIX: "mautic/plugins"
  INCREMENTAL_MODE: "false"
