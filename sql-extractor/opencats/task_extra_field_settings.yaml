# OpenCATS Extra Field Settings Table Extraction
# Custom field configurations

task_id: extract_extra_field_settings
job_name: sql-extractor

environment:
  TABLE_NAME: "extra_field_settings"
  OUTPUT_PREFIX: "opencats/extra_field_settings"
  INCREMENTAL_MODE: "false"
